const calc = (e) => {
    e.preventDefault()
    
    const altura = document.querySelector("#diametro").value

     const pi = Math.PI 
     
    const resultado = 

    res.textContent = `resultado = ${resultado}`
}

const btn = document.querySelector("#btn")

btn.addEventListener("click", calc)